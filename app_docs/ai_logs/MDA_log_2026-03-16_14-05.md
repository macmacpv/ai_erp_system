# MDA Log
Date: 2026-03-16 14:05 UTC
Iteration: 19

## Actions Executed
1.  **Database Migration**: Created `V7__add_personnel_permissions.sql` wprowadzającą uprawnienia `erp.personnel.read`, `create`, `edit` oraz `delete`.
2.  **Tłumaczenia (I18N)**: Uzupełniono plik `messages.properties` o komplet kluczy dla widoków Personelu i powiadomień błędów.
3.  **Warstwa Usług**: Utworzono `PersonnelService` z metodami `@Transactional` w celu zapewnienia spójności danych przy tworzeniu kont (`Account`) powiązanych bezpośrednio z profilami pracowników (`Employee`). Dodano funkcję wykrywania i raportowania zajętych nazw użytkownika z użyciem wyjątków.
4.  **Kontrola dostępu oparta na ACE**: 
    - Widok `PersonnelPage` od teraz weryfikuje posiadanie uprawnienia `erp.personnel.read` na etapie konstruktora. Jeśli jego brak, renderuje komunikat "Brak dostępu".
    - Przyciski "Nowy Pracownik", "Edytuj" oraz "Usuń" renderują się wyłącznie, jeśli aktualnie zalogowany użytkownik ma dostęp do poszczególnych "węzłów" uprawnień.
5.  **Interfejs Użytkownika (UI)**: 
    - Zastąpiono statyczne teksty wywołaniami `getTranslation(...)`.
    - Zaimplementowano okno dialogowe pozwalające na bezpieczne dodawanie i edycję pracownika wraz z ustanawianiem, bądź ignorowaniem hasła konta.
    - Zoptymalizowano proces odświeżania zakładek "Roster", aby nie renderować całej zawartości widoku od zera, a jedynie odświeżać stan danych w siatkach.

## Status
* **Current State**: 🟣 **STATE 3: OPERATIONAL**.
* **Next Step**: Kontynuacja budowy modułu ERP (logistyka, szkolenia) według instrukcji HP.